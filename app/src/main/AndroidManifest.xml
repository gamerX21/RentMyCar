<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tn.krh.rentmycar">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".homeApp.Agency.EditCarActivity"></activity>
        <activity android:name=".homeApp.Agency.AddNewActivity" >
        </activity>
        <activity android:name=".homeApp.Agency.HomeAgencyActivity"></activity>
        <activity android:name=".main.SignUp.SpinnerSignUpActivity" />
        <activity android:name=".homeApp.Client.AvailableAgenceCarsActivity" />
        <activity android:name=".homeApp.Client.DetailsActivity" />
        <activity android:name=".homeApp.Client.ProfileActivity" />
        <activity android:name=".homeApp.Client.HomeClientActivity" />
        <activity android:name=".main.SignupActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>